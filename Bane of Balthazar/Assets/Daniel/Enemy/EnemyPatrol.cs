using System.Collections;
using System.Collections.Generic;
using EnemyBase;
using UnityEngine;

public class EnemyPatrol : MonoBehaviour
{   /*
    public Transform[] patrolDestinations;
    public int patrolPoint;
    [SerializeField] public int finalDestination;


    private void OnTriggerEnter(Collider destinationReached)
    {
        if (destinationReached.GetComponent<BasicEnemy>() != null)
        {   
            if(patrolPoint > finalDestination) // if Enemy has finished patrolling, reset route!
            {
                patrolPoint = 0;
                Debug.Log("Reset patrol!");
            }

            
            patrolPoint++;
        }
    }
    */
}
